impl crate::app::WavesPreviewer {
    #[allow(dead_code)]
    pub(in crate::app) fn ui_editor_view(&mut self, _ui: &mut egui::Ui, _ctx: &egui::Context, _tab_idx: usize) {
        // Placeholder: full extraction of editor canvas/inspector can follow.
    }
}

